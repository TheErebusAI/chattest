MODEL_CONFIG = {
    # Configuration for model_1
    "model_1": {
        "key": "model_1",  # Unique identifier for the model
        "url": "https://api.example.com/v1/completions",  # URL for the model's API endpoint
        "apiKey": "example-api-key",  # API key for authentication (optional)
        "apiKeyHeader": "Authorization"  # Header name for the API key (optional)
    },
    # Configuration for model_2
    "model_2": {
        "key": "model_2",  # Unique identifier for the model
        "url": "https://api.anotherexample.com/v1/completions",  # URL for the model's API endpoint
        "apiKey": "another-example-api-key",  # API key for authentication (optional)
        "apiKeyHeader": "Authorization"  # Header name for the API key (optional)
    }
}
